# Example file for contributors / deployment
# Rename to ".env" and adjust the value for your backend

# Local development (via Vite proxy)
VITE_API_BASE_URL=/api

# Production (example)
# VITE_API_BASE_URL=https://your-deployed-backend.onrender.com/api